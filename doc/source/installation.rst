============
Installation
============

At the command line::

    $ pip install fuel-dev-tools

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv fuel-dev-tools
    $ pip install fuel-dev-tools
